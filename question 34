CODE
#include<stdio.h>
int main()
{
   int  n,i,sum1=0,sum2=0;
   printf("Enter dimension(even) of the array");
   scanf("%d",&n);
   int a[n];
   printf("Enter a[%d] array",n);
   for(i=0;i<n;i++)
    scanf("%d",&a[i]);
   for(i=0;i<n/2;i++)
    sum1=sum1+a[i];
   for(i=n/2;i<n;i++)
    sum2=sum2+a[i];
   if(sum1<=sum2)
    printf("add %d",sum2-sum1);
   else
    printf("add %d",sum1-sum2);
   return 0;
}
OUTPUT
Enter dimension(even) of the array6
Enter a[6] array1
2
1
2
1
3
add 2
