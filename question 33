CODE
#include<stdio.h>
int main()
{
   int n,i,sum=0;
   printf("Enter dimension of the array");
   scanf("%d",&n);
   int a[n],b[n];
   printf("Enter the a[%d] array",n);
   for(i=0;i<n;i++)
    scanf("%d",&a[i]);
   for(i=0;i<n;i++)
    {
        sum=sum+a[i];
        b[i]=sum/(i+1);
    }
    for(i=0;i<n;i++)
     printf("%d ",b[i]);
   return 0;
}
OUTPUT
Enter dimension of the array5
Enter the a[5] array10
20
30
40
50
10 15 20 25 30 
